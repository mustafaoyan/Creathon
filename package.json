{
  "name": "rubrix",
  "private": true,
  "packageManager": "pnpm@9.15.0",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev:api": "pnpm --filter @rubrix/api dev",
    "dev:web": "pnpm --filter @rubrix/web dev",
    "build": "pnpm -r --filter=./apps/* build",
    "typecheck": "pnpm -r --filter=./apps/* typecheck",
    "lint": "pnpm -r --filter=./apps/* lint",
    "db:generate": "pnpm --filter @rubrix/api db:generate",
    "db:migrate:local": "pnpm --filter @rubrix/api db:migrate:local",
    "db:migrate:remote": "pnpm --filter @rubrix/api db:migrate:remote"
  }
}
